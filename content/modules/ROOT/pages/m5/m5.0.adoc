:imagesdir: ../../assets/images
:page-time: 10
include::partial$style.adoc[]

[id='lab5-protect-llm-consumption-with-connectivity-link']
== Lab 5 - Protect LLM consumption with Connectivity Link

== Introduction

In the previous phases of this workshop, you connected disparate chat platforms into a unified communication hub, securely ingested and transformed client data, and introduced an AI agent that helps support agents resolve invoice issues through natural language -- all powered by _Apache Camel_, _Kafka_, and Model-as-a-Service (MaaS).

But providing internal teams with access to Large Language Models introduces new challenges. AI resource costs are tied to **token consumption** rather than simple API hits. Without proper governance, there is no way to prevent system abuse or control runaway expenses. Traditional rate limiting -- which simply counts requests -- is not enough.

In this final phase, you will place a **governance layer** in front of the LLM using _Red Hat Connectivity Link_. You will:

* **AuthPolicy** -- Control _who_ can access the LLM API using API key-based authentication and group-based authorization.
* **TokenRateLimitPolicy** -- Control _how much_ each user can consume by enforcing rate limits based on actual LLM token usage.

By the end of this module, the AI agent will be fully secured with tiered access control -- _free_ users get a limited token budget, while _gold_ users enjoy a higher allowance.

{empty} +

=== Your Environment

* Login to the link:https://developers.redhat.com/products/codeready-workspaces/overview[Red Hat OpenShift Dev Spaces, window="_blank"]environment you have been using throughout the workshop using the same credentials as before. {user-username}.

* Start a new terminal session in your Dev Spaces IDE if you already donot have one open.
+
image::intro/toggle-terminal.png[width=40%]

* Verify you are in the correct namespace `{user-username}-devspaces` by running:
+
[.copypaste]
----
oc project{user-username}-devspaces
----

== Introducing Red Hat Connectivity Link

=== What is Connectivity Link?

_Red Hat Connectivity Link_ is a _Kubernetes_-native solution for managing, securing, and controlling API traffic. Built on the _Kuadrant_ project, it integrates directly with the _Gateway API_ to enforce policies at the gateway level and HTTP Route Level -- before requests ever reach the LLM backend.

{empty} +

=== Gateway API Architecture

_Connectivity Link_ operates at the _Gateway API_ layer -- a _Kubernetes_ standard for managing ingress traffic. It uses a layered model:

[source]
----
GatewayClass          <-- Defines the controller (e.g., OpenShift Gateway Controller)
    |
    v
Gateway               <-- The actual listener (port, protocol, namespace scope)
    |
    v
HTTPRoute             <-- Routing rules (path matching, header modification, backend targets)
----

_Connectivity Link_ attaches its policies (AuthPolicy, TokenRateLimitPolicy) directly to these _Gateway API_ resources. This means security and governance are enforced **at the infrastructure level**, without modifying application code or the AI agent itself.

{empty} +

=== End-to-End Request Flow

The following diagram shows how a request flows from the chat interface through the governance layer to the LLM backend:

[source]
----
                      +---------------------+
                      |   Chat Interface    |
                      | (Bearer API Key)    |
                      +---------+-----------+
                                |
                                v
                      +---------------------+
                      |      Gateway        |
                      | (internal-gateway)  |
                      +---------+-----------+
                                |
                                v
                  +---------------------------+
                  |        HTTPRoute          |
                  |    (ai-proxy-route)       |
                  |                           |
                  |  +---------------------+  |
                  |  |     AuthPolicy      |  |
                  |  | (API key + groups)  |  |
                  |  +---------------------+  |
                  |                           |
                  |  +---------------------+  |
                  |  | TokenRateLimitPolicy|  |
                  |  | (per-user token     |  |
                  |  |  budget)            |  |
                  |  +---------------------+  |
                  +------------+--------------+
                               |
                               v
                      +---------------------+
                      |       LLM API       |                
                      +---------------------+
----

// TODO: Replace ASCII diagram with a proper architecture diagram screenshot
image::m5/phase4_architecture_diagram.png[Phase 4 Architecture Diagram - placeholder,width=80%]

{empty} +

== Infrastructure Provisioned for This Phase

The following resources have been set up for you so you can focus on the policy configuration:

[cols="1,2", options="header"]
|===
| Resource | Purpose

| **GatewayClass, Gateway, HTTPRoute**
| The _Gateway API_ stack that routes traffic from the chat interface to the external LLM backend.

| **Kuadrant Operator & System**
| The _Connectivity Link_ operator, installed in the `kuadrant-system` namespace.

| **User Secrets**
| API key secrets for `free` and `gold` tier users (details below).
|===

{empty} +

=== Verify Your Environment

Confirm everything is ready before starting the labs:

{blank}

. From the terminal in your Dev Spaces IDE, run the following command to check that gateway is up and running:
+
[.copypaste]
----
oc get gateway internal-gateway -n ai-gateway
----
+
{empty} +

. You should see output confirming the gateway exists and is in a ready state, similar to:
+
----
NAME               CLASS               ADDRESS                                                           PROGRAMMED   AGE
internal-gateway   openshift-default   internal-gateway-openshift-default.ai-gateway.svc.cluster.local   True         14h
----

. Each user has http route `ai-proxy-route` in their namespace `{user-username}-ns` that targets the LLM API. Run the following command to verify it is present
+
[.copypaste]
----
oc describe httproute -n {user-username}-devspaces | grep -E "Name:|Backend|Hostnames"
----
+
This command list the HTTPRoute details including the backend reference to the LLM API and the gateway it is attached to (which is the `internal-gateway` that you verified earlier). 

. Look for output confirming the HTTPRoute exists and is correctly configured, similar to:
+
----
Name:         ai-proxy-route
    Name:       internal-gateway
    Backend Refs:
      Name:       ai-api-external
          Name:   Host
          Name:   Authorization
    Controller Name:         openshift.io/gateway-controller/v1
      Name:       internal-gateway
----






[.verification]
****
Were you able to list the `internal-gateway` Gateway and `ai-proxy-route` HTTPRoute ?

[.success]
Your environment is ready. Proceed to Next step.

[.fail]
Contact the workshop facilitator to ensure your namespace is properly provisioned.
****

{empty} +

=== Understanding the User Secrets

Two API key secrets have been pre-created. Understanding their structure is important because both policies reference the labels and annotations on these secrets.

**Free-tier user:**
----
apiVersion: v1
kind: Secret
metadata:
  name: trlp-tutorial-api-key-free-{user-username}
  namespace: kuadrant-system
  labels:
    authorino.kuadrant.io/managed-by: authorino
    app: my-llm
  annotations:
    kuadrant.io/groups: free
    secret.kuadrant.io/user-id: {user-username}
stringData:
  api_key: iamafreeuser
type: Opaque
----

Key fields in this secret:

* `authorino.kuadrant.io/managed-by: authorino` -- Marks this secret as managed by _Authorino_ (the auth engine behind _Connectivity Link_).
* `app: my-llm` -- Label selector -- the AuthPolicy uses this to discover valid API keys.
* `kuadrant.io/groups: free` -- Group membership -- determines which rate limit tier applies.
* `secret.kuadrant.io/user-id: {user-username}` -- Unique user ID -- used as a counter key for per-user rate limiting.

The **gold-tier** secret follows the same structure but with `kuadrant.io/groups: gold`,`api_key: iamagolduser`.

{empty} +

== Securing the API with Authentication

Now that you understand the architecture, it's time to lock down the LLM endpoint. You will create an **AuthPolicy** that is applied to the `ai-proxy-route` HTTPRoute and enforces:

1. **API Key Authentication** -- Only requests with a valid Bearer token (matching a known secret) are allowed.
2. **Group-based Authorization** -- Only users belonging to the `free` or `gold` groups can access the API.
3. **Identity Extraction** -- The user's group and user ID are extracted from the secret's annotations and passed downstream for use by the TokenRateLimitPolicy.

{empty} +

=== Step 1: Review and Apply the AuthPolicy

Examine the AuthPolicy and apply it by pasting the following command in your terminal:

[.copypaste]
****
oc apply -f - <<EOF
apiVersion: kuadrant.io/v1
kind: AuthPolicy
metadata:
  name: ai-proxy-auth
  namespace: {user-username}-devspaces
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: ai-proxy-route
  rules:
    authentication:
      api-key-users:
        apiKey:
          selector:
            matchLabels:
              app: my-llm
        credentials:
          authorizationHeader:
            prefix: Bearer
    response:
      success:
        filters:
          identity:
            json:
              properties:
                groups:
                  selector: auth.identity.metadata.annotations.kuadrant\.io/groups
                userid:
                  selector: auth.identity.metadata.annotations.secret\.kuadrant\.io/user-id
    authorization:
      allow-groups:
        opa:
          rego: |
            groups := split(object.get(input.auth.identity.metadata.annotations, "kuadrant.io/groups", ""), ",")
            allow { groups[\_] == "free" }
            allow { groups[\_] == "gold" }
EOF
****

Key fields in this policy:

* `namespace: {user-username}-ns` -- The policy is created in your namespace.
* `name: ai-proxy-route` -- Targets the `ai-proxy-route` HTTPRoute -- any request matching this route must pass authentication.
* `app: my-llm` -- Looks for _Kubernetes_ Secrets with this label to find valid API keys.
* `prefix: Bearer` -- Expects the API key in the `Authorization` header (e.g., `Authorization: Bearer iamafreeuser`).
* `kuadrant\.io/groups` -- Extracts the group annotation from the matched secret and exposes it as `groups` in the response filter.
* `secret\.kuadrant\.io/user-id` -- Extracts the user ID annotation and exposes it as `userid`.
* `rego` -- OPA (Open Policy Agent) rule that only allows users whose group is `free` or `gold`.

{empty} +


{empty} +

=== Step 2: Verify the AuthPolicy

{blank}

. From the devspaces terminal, run the following command to check that the AuthPolicy was created successfully:
+
[.copypaste]
----
oc describe authpolicy -n {user-username}-devspaces | grep -E "Name:|Target Ref:|Type:|Status:"
----

. You should see output confirming the AuthPolicy exists, is targeting the correct HTTPRoute, and is in an enforced state, similar to:
----
Name:         ai-proxy-auth
  Target Ref:
    Name:   ai-proxy-route
Status:
    Status:                True
    Type:                  Accepted
    Status:                True
    Type:                  Enforced
----
{empty} +

{empty} +

[.verification]
****
Does the `ai-proxy-auth` AuthPolicy show status **True** and Type **Enforced** ?

[.success]
The AuthPolicy is enforced. Proceed to test authentication.

[.fail]
Check that you pasted the correct YAML and that the namespace is set to `{user-username}-ns`.
****

{empty} +

=== Step 3: Test Authentication via the Chat Interface

Now test that the AuthPolicy is working using the same chat interface from Phase 3.

==== Configuring the API Key

To authenticate with the LLM, set your API key in the agent configuration file you are already familiar with:

{blank}

. Open the `forage-agent-factory.properties` file.
+
{empty} +

. Locate the `forage.myagent.agent.api.key` property.
+
{empty} +

. Set the value to the API key you want to test with.

Example:

[.copypaste]
****
forage.myagent.agent.api.key=iamafreeuser
****

// TODO: Add screenshot showing the properties file with the API key field
image::m5/chat_interface_apikey_config.png[Chat interface API key configuration - placeholder,width=80%]

{empty} +

==== Test Scenarios

Test the following scenarios by updating the API key value and sending a message through the chat interface:

{blank}

. **Invalid credentials** -- Set an invalid API key:
+
[.copypaste]
****
forage.myagent.agent.api.key=invalidkey
****
+
Send a message. The request should be **denied** (401 Unauthorized).
+
{empty} +

. **Valid free-tier key** -- Set the free-tier API key:
+
[.copypaste]
****
forage.myagent.agent.api.key=iamafreeuser
****
+
Send a message. The request should be **allowed** -- you receive a response from the LLM.

// TODO: Add screenshot showing a successful LLM response in the chat interface
image::m5/auth_test_success.png[Successful authentication test - placeholder,width=80%]

// TODO: Add screenshot showing a denied request (401) in the chat interface
image::m5/auth_test_denied.png[Failed authentication test - placeholder,width=80%]

{empty} +

At this point, authentication and authorization are enforced. Only users with valid API keys belonging to the `free` or `gold` groups can access the LLM.

{empty} +

[.verification]
****
Did the invalid key return a 401 error, and the valid free-tier key return a successful LLM response?

[.success]
Authentication and authorization are working correctly.

[.fail]
Review the AuthPolicy YAML and ensure the API key secrets exist in `kuadrant-system`.
****

{empty} +

== Enforcing Token Budgets with Rate Limiting

Now that access is secured, you will add a **TokenRateLimitPolicy** to control how many LLM tokens each user can consume. This is the key differentiator for AI workloads -- unlike traditional rate limiting (which counts requests), token rate limiting counts the actual **tokens used** in LLM responses, giving you fine-grained cost control over AI consumption.

{empty} +

=== Step 1: Review and Apply the AuthPolicy
Examine the TokenRateLimitPolicy and apply it by pasting the following command in your terminal:

[.copypaste]
****
oc apply -f - <<EOF
apiVersion: kuadrant.io/v1alpha1
kind: TokenRateLimitPolicy
metadata:
  name: openai-token-limits
  namespace: {user-username}-devspaces
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: ai-proxy-route
  limits:
    free:
      rates:
        - limit: 50
          window: 1m
      when:
        - predicate: request.path == "/v1/chat/completions"
        - predicate: |
            auth.identity.groups.split(",").exists(g, g == "free")
      counters:
        - expression: auth.identity.userid
    gold:
      rates:
        - limit: 200
          window: 1m
      when:
        - predicate: request.path == "/v1/chat/completions"
        - predicate: |
            auth.identity.groups.split(",").exists(g, g == "gold")
      counters:
        - expression: auth.identity.userid
EOF
****

Key fields in this policy:

* `namespace: {user-username}-ns` -- Created in your namespace.
* `name: ai-proxy-route` -- Targets the same HTTPRoute as the AuthPolicy.
* `free` -- **Free tier** -- limited to **50 tokens per minute**.
* `request.path == "/v1/chat/completions"` -- Only applies to requests hitting the chat completions path.
* `auth.identity.groups` -- Matches users whose `groups` identity property (set by the AuthPolicy response filter) contains `free` or `gold`.
* `auth.identity.userid` -- Rate limit counter is **per user** -- each user gets their own token budget.
* `gold` -- **Gold tier** -- limited to **200 tokens per minute**, same structure as free but with a higher allowance.

[IMPORTANT]
====
Notice how the TokenRateLimitPolicy depends on the AuthPolicy:

* The `auth.identity.groups` and `auth.identity.userid` fields referenced here are populated by the AuthPolicy's `response.success.filters` section.
* Without the AuthPolicy, the TokenRateLimitPolicy would have no identity context to evaluate.
====

{empty} +



=== Step 3: Verify the TokenRateLimitPolicy

. From the devspaces terminal, run the following command to check that the AuthPolicy was created successfully:
+
[.copypaste]
----
oc describe tokenratelimitpolicy -n {user-username}-devspaces | grep -iE "Name:|target|free:|gold:|limit:|window:|type:|status:"
----

. You should see output confirming the AuthPolicy exists, is targeting the correct HTTPRoute, and is in an enforced state, similar to:
----
Name:         openai-token-limits
    Free:
        Limit:   50
        Window:  1m
    Gold:
        Limit:   200
        Window:  1m
  Target Ref:
    Name:   ai-proxy-route
Status:
    Status:                True
    Type:                  Accepted
    Status:                True
    Type:                  Enforced
----



[.verification]
****
Does the  TokenRateLimitPolicy show status **Enforced**?

[.success]
The token rate limit policy is active. Proceed to test token limits.

[.fail]
Check that you pasted the correct YAML and that the namespace is set to `{user-username}-ns`.
****

{empty} +

=== Step 4: Test Token Rate Limiting via the Chat Interface

Use the chat interface to test the rate limiting behavior.

{empty} +

==== Test 1: Free-tier token limits

{
. Send a message that asks the LLM to generate a longer response (e.g., _"Explain Kubernetes in 500 words"_).
+
{empty} +

. Continue sending the same messages in quick succession.
+
{empty} +

. After consuming **50 tokens within a minute**, your next request should be **rate limited** (429 Too Many Requests).
+
{empty} +

. Wait for the 1-minute window to reset, then try again -- the request should succeed.

// TODO: Add screenshot showing a successful response before rate limit is hit
image::m5/trlp_free_before_limit.png[Free-tier response before rate limit - placeholder,width=80%]

// TODO: Add screenshot showing the 429 rate limit response for a free-tier user
image::m5/trlp_free_rate_limited.png[Free-tier rate limited response - placeholder,width=80%]

{empty} +

==== Test 2: Gold-tier token limits

{blank}

. Update `forage-agent-factory.properties`:
+
[.copypaste]
****
forage.myagent.agent.api.key=iamagolduser
****
+
{empty} +

. Send similar messages to the LLM using the chat interface in quick succession.
+
{empty} +

. Notice that you can consume up to **200 tokens per minute** before being rate limited -- 4x the free-tier allowance.

// TODO: Add screenshot showing gold-tier user with higher token allowance
image::m5/trlp_gold_higher_limit.png[Gold-tier higher token allowance - placeholder,width=80%]

{empty} +

[NOTE]
====
Rate limits are tracked **per user** using the `userid` counter from the AuthPolicy. Each user gets their own independent token budget -- one user hitting their limit does not affect another user's quota.
====

{empty} +

[.verification]
****
Did the free-tier user hit a 429 rate limit after ~50 tokens, while the gold-tier user could consume up to ~200 tokens per minute?

[.success]
Congratulations! You have successfully implemented tiered token-based rate limiting.

[.fail]
Ensure both the AuthPolicy and TokenRateLimitPolicy show Enforced status, and try again with a fresh 1-minute window.
****

{empty} +

== Summary

Congratulations! You have completed the final phase of the workshop, adding a governance layer on top of the AI agent you built across the previous phases.

=== Key Takeaways

* **_Connectivity Link_ policies attach to _Gateway API_ resources** (HTTPRoutes, Gateways) -- no changes to the AI agent or application code required.
* **Policies compose together** -- the AuthPolicy extracts identity information that the TokenRateLimitPolicy uses for per-user, per-tier rate limiting.
* **Token-based rate limiting** is purpose-built for AI/LLM workloads, giving you cost control based on actual token consumption rather than simple request counts.
* **Everything is declarative and _Kubernetes_-native** -- policies are YAML resources managed with `kubectl`, making them easy to version, review, and automate.
* **Integration tools provide the foundation for AI** -- As you've seen across all four phases, robust connectivity tools like _Apache Camel_, _Kafka_, Service Interconnect, and _Connectivity Link_ provide the essential, secure foundation needed to successfully weave AI into everyday business workflows.
