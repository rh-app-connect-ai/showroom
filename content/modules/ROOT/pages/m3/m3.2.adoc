:imagesdir: ../../assets/images
:page-time: 10
include::partial$style.adoc[]

[id='lab3-service-interconnect']
== Enabling Database Connectivity

You have a Camel route ready to query the database — but the database isn't reachable yet. Let's find out why, and enable the connection.

{empty} +

=== Why can't you reach the database?

The backend database is hosted in an isolated namespace (`shared-database`) with strict network policies — no direct access is allowed from other namespaces.

[.mermaid]
[subs="attributes"]
----
flowchart LR
    subgraph yoursLabel ["{username}-devspaces"]
        subgraph yours [" "]
            Client[DB Client]
        end
    end

    Client -.-x|"❌ unreachable"| SVC

    subgraph sharedLabel [shared-database]
        subgraph shared [" "]
            SVC((svc))
            SVC --- DB[(PostgreSQL)]
        end
    end

    style yoursLabel fill:none,stroke:none
    style sharedLabel fill:none,stroke:none
    style DB fill:#336,color:#fff
----

Try it yourself — use `curl` as a simple way to test connectivity with the database:

{blank}

[.copypaste]
****
curl postgres.shared-database.svc:5432 \
-v --connect-timeout 3
****

[TIP]
--
.What does this command do?
[%collapsible]
====
* `postgres.shared-database.svc:5432` — target service, namespace, and PostgreSQL port
* `-v` — verbose, shows the connection attempt
* `--connect-timeout 3` — gives up after 3 seconds instead of hanging
====
--

The connection will time out — the database is not directly accessible from your namespace:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">*   Trying 172.31.163.169:5432...
* Connection timed out after 3001 milliseconds
* Closing connection 0
curl: (28) Connection timed out after 3001 milliseconds</pre>
++++

{empty} +

=== Bridging namespaces with Service Interconnect

{rhsi} solves this by creating encrypted application-level connections between namespaces — without requiring VPNs, firewall rules, or exposing services to the public network.

Once configured, a local service is created in your namespace that transparently forwards traffic to the remote database:

{blank}

[.mermaid]
[subs="attributes"]
----
flowchart LR
    subgraph yoursLabel ["{username}-devspaces"]
        subgraph yours [" "]
            Client[DB Client]
            SI1{{"Service\nInterconnect"}}
        end
    end

    Client --> SI1

    SI1 -.-|"🔒 secure link"| SI2

    subgraph sharedLabel [shared-database]
        subgraph shared [" "]
            SI2{{"Service\nInterconnect"}}
            SVC((svc))
            SVC --- DB[(PostgreSQL)]
        end
    end

    SI2 --> SVC

    style yoursLabel fill:none,stroke:none
    style sharedLabel fill:none,stroke:none
    style DB fill:#336,color:#fff
----

{empty} +

From your application's perspective, the database will appear as a local service — no special configuration needed.

{empty} +

=== Under the hood

Service Interconnect uses a set of resources to establish and manage the connection. Here's how the previous diagram breaks down:

{blank}

[.mermaid]
[subs="attributes"]
----
flowchart LR
    subgraph yoursLabel ["{username}-devspaces"]
        subgraph yours [" "]
            Client[DB Client]
            SVC2((svc))
            subgraph S2 ["Site(2)"]
                Listener((Listener))
                Grant[AccessGrant]
            end
        end
    end

    Client --> SVC2 --> Listener

    S2 -.-|"🔒 secure link"| S1

    subgraph sharedLabel [shared-database]
        subgraph shared [" "]
            subgraph S1 ["Site(1)"]
                Connector((Connector))
                Token[AccessToken]
            end
            SVC((svc))
            SVC --- DB[(PostgreSQL)]
        end
    end

    Connector --> SVC
    Grant ~~~ Token

    style yoursLabel fill:none,stroke:none
    style sharedLabel fill:none,stroke:none
    style S2 fill:#e8eaf6,stroke:#7986cb
    style S1 fill:#e8eaf6,stroke:#7986cb
    style Grant fill:#fff,stroke:#7986cb
    style Listener fill:#fff,stroke:#7986cb
    style Token fill:#fff,stroke:#7986cb
    style Connector fill:#fff,stroke:#7986cb
    style DB fill:#336,color:#fff
----

{empty} +

Each side runs a *Site* — the foundation that enables the Skupper network. The connection between them is established through an *AccessGrant* and *AccessToken* pair. Traffic flows from the *Listener* in your namespace to the *Connector* on the database side.

NOTE: The database lives in a secure, air-tight environment — no inbound ports are exposed. To keep it that way, the link is initiated *from* the database side *outward* to your site. This is why you create an AccessGrant on your side: it gives the database site permission to connect to you and establish a bidirectional communication link. Its environment never opens an entrypoint that could be exploited.

{empty} +

=== What you will do

The server side has been partially configured by your platform team:

* A *Site* in the `shared-database` namespace
* A *Connector* that bridges the Skupper network to the PostgreSQL service on port `5432`

Your job is to complete the client side:

. Create a *Site* in your namespace with `linkAccess` enabled
. Generate an *AccessGrant* so the database namespace can connect to you
. Create an *AccessToken* in the database namespace to establish the link
. Create a *Listener* to expose the remote database as a local service

{blank}

[.mermaid]
[subs="attributes"]
----
flowchart LR
    subgraph yoursLabel2 ["{username}-devspaces"]
        subgraph yours2 [" "]
            Client[DB Client]
            SVC2((svc))
            subgraph S2 ["✏️ Site(2)"]
                Listener(("✏️ Listener"))
                Grant["✏️ AccessGrant"]
            end
        end
    end

    Client --> SVC2 --> Listener

    S2 -.-|"🔒 secure link"| S1

    subgraph sharedLabel2 [shared-database]
        subgraph shared2 [" "]
            subgraph S1 ["✅ Site(1)"]
                Connector(("✅ Connector"))
                Token["✏️ AccessToken"]
            end
            SVC((svc))
            SVC --- DB[(PostgreSQL)]
        end
    end

    Connector --> SVC
    Grant ~~~ Token

    style yoursLabel2 fill:none,stroke:none
    style sharedLabel2 fill:none,stroke:none
    style S2 fill:#ffe0b2,stroke:#fb8c00
    style S1 fill:#c8e6c9,stroke:#66bb6a
    style Listener fill:#fff3e0,stroke:#fb8c00
    style Grant fill:#fff3e0,stroke:#fb8c00
    style Token fill:#fff3e0,stroke:#fb8c00
    style Connector fill:#e8f5e9,stroke:#66bb6a
    style DB fill:#336,color:#fff
----

[.indent2]
✅ Already configured by the platform team +
✏️ Your tasks

{empty} +

=== Step 0: Explore the Server Side (Optional)

You have read-only access to the `shared-database` namespace. Take a moment to inspect what the platform team has set up.

View the Skupper resources:

{blank}

[.copypaste]
****
oc get site,connector -n shared-database
****

You should see output similar to:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">NAME                            STATUS   SITES IN NETWORK   MESSAGE
site.skupper.io/database-site   Ready    1                  OK

NAME                            ROUTING KEY   PORT   HOST   SELECTOR       STATUS    HAS MATCHING LISTENER   MESSAGE
connector.skupper.io/postgres   postgres      5432          app=postgres   Pending                           No matching listeners</pre>
++++

Notice the Connector status is `Pending` with `No matching listeners` — that's expected. You will create the matching Listener later in this exercise.

These are the two pre-configured resources from the diagram. Here's what you need to complete:

{blank}

[.mermaid]
----
flowchart LR
    S1["✅ Site(1)"] --> C["✅ Connector"] --> S2["✏️ Site(2)"] --> G["✏️ AccessGrant"] --> T["✏️ AccessToken"] --> L["✏️ Listener"]
    style S1 fill:#e8f5e9,stroke:#66bb6a
    style C fill:#e8f5e9,stroke:#66bb6a
    style S2 fill:#fff3e0,stroke:#fb8c00
    style G fill:#fff3e0,stroke:#fb8c00
    style T fill:#fff3e0,stroke:#fb8c00
    style L fill:#fff3e0,stroke:#fb8c00
----

{empty} +

=== Step 1: Create a Site in Your Namespace

Create a Site with `linkAccess` enabled. This makes your namespace a *listening site* that can accept incoming connections from the database.

{blank}

[.copypaste]
****
cat <<EOF | oc apply -f -
apiVersion: skupper.io/v2alpha1
kind: Site
metadata:
  name: {username}-site
  namespace: {username}-devspaces
spec:
  linkAccess: route
EOF
****

Verify the Site is ready:

{blank}

[.copypaste]
****
oc get site {username}-site -n {username}-devspaces
****

Wait until the `STATUS` column shows `Ready`:

{blank}

[subs=attributes]
++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">NAME         STATUS   SITES IN NETWORK   MESSAGE
{username}-site   Ready    1                  OK</pre>
++++

Your Site is ready. Here's your progress:

{blank}

[.mermaid]
----
flowchart LR
    S1["✅ Site(1)"] --> C["✅ Connector"] --> S2["✅ Site(2)"] --> G["✏️ AccessGrant"] --> T["✏️ AccessToken"] --> L["✏️ Listener"]
    style S1 fill:#e8f5e9,stroke:#66bb6a
    style C fill:#e8f5e9,stroke:#66bb6a
    style S2 fill:#e8f5e9,stroke:#66bb6a
    style G fill:#fff3e0,stroke:#fb8c00
    style T fill:#fff3e0,stroke:#fb8c00
    style L fill:#fff3e0,stroke:#fb8c00
----

{empty} +

=== Step 2: Create an AccessGrant

Before jumping in, remember that the link is initiated *from* Site(1) (database) *to* Site(2) (yours). Here's how the grant and token work together to make that happen:

{blank}

[.mermaid]
----
sequenceDiagram
    actor A as Admin<br/>(You)
    participant S2 as Site(2)
    participant S1 as Site(1)

    A-->>S2: Create AccessGrant

    Note over S2: AccessGrant

    A-->>S1: Create AccessToken

    Note over S1: AccessToken

    S1-->>S2: Redeem token
    S2-->>S1: Transfer link details
    S1->>S2: Create link

    Note over S2,S1: 🔒 secure link established
----

You create an AccessGrant on your Site(2) and an AccessToken on Site(1). The database site then redeems the token to establish a secure link back to you.

{blank}

Now, create the AccessGrant:

{blank}

[.copypaste]
****
cat <<EOF | oc apply -f -
apiVersion: skupper.io/v2alpha1
kind: AccessGrant
metadata:
  name: grant-{username}
  namespace: {username}-devspaces
spec:
  redemptionsAllowed: 1
  expirationWindow: 168h
EOF
****

Wait for the AccessGrant to be ready and retrieve its details:

{blank}

[.copypaste]
****
oc get accessgrant grant-{username} -n {username}-devspaces
****

Wait until the `STATUS` column shows `Ready`:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">NAME            REDEMPTIONS ALLOWED   REDEMPTIONS MADE   EXPIRATION             STATUS   MESSAGE
grant-userN     1                                        2026-03-27T11:29:08Z   Ready    OK</pre>
++++

Your AccessGrant is ready. Here's your progress:

{blank}

[.mermaid]
----
flowchart LR
    S1["✅ Site(1)"] --> C["✅ Connector"] --> S2["✅ Site(2)"] --> G["✅ AccessGrant"] --> T["✏️ AccessToken"] --> L["✏️ Listener"]
    style S1 fill:#e8f5e9,stroke:#66bb6a
    style C fill:#e8f5e9,stroke:#66bb6a
    style S2 fill:#e8f5e9,stroke:#66bb6a
    style G fill:#e8f5e9,stroke:#66bb6a
    style T fill:#fff3e0,stroke:#fb8c00
    style L fill:#fff3e0,stroke:#fb8c00
----

{empty} +

=== Step 3: Create an AccessToken in the Database Namespace

Now create an AccessToken in the `shared-database` namespace to establish the link from the database to your namespace. The token must be named `token-{username}`.

Extract the grant details and create the AccessToken:

{blank}

[.copypaste]
****
URL=$(oc get accessgrant grant-{username} -n {username}-devspaces -o jsonpath='{.status.url}')
CODE=$(oc get accessgrant grant-{username} -n {username}-devspaces -o jsonpath='{.status.code}')
CA=$(oc get accessgrant grant-{username} -n {username}-devspaces -o jsonpath='{.status.ca}' | sed 's/^/    /')

cat <<EOF | oc apply -f -
apiVersion: skupper.io/v2alpha1
kind: AccessToken
metadata:
  name: token-{username}
  namespace: shared-database
spec:
  url: "${URL}"
  code: "${CODE}"
  ca: |
${CA}
EOF
****

IMPORTANT: The AccessToken name *must* follow the pattern `token-<your-username>`. Any other name will be rejected.

After a few moments, a *Link* resource is automatically created in `shared-database`. Verify:

{blank}

[.copypaste]
****
oc get link -n shared-database
****

Wait until the link status shows `Ready`:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">NAME            STATUS   REMOTE SITE    MESSAGE
token-userN     Ready    userN-site     OK</pre>
++++

Almost there — one more resource to go:

{blank}

[.mermaid]
----
flowchart LR
    S1["✅ Site(1)"] --> C["✅ Connector"] --> S2["✅ Site(2)"] --> G["✅ AccessGrant"] --> T["✅ AccessToken"] --> L["✏️ Listener"]
    style S1 fill:#e8f5e9,stroke:#66bb6a
    style C fill:#e8f5e9,stroke:#66bb6a
    style S2 fill:#e8f5e9,stroke:#66bb6a
    style G fill:#e8f5e9,stroke:#66bb6a
    style T fill:#e8f5e9,stroke:#66bb6a
    style L fill:#fff3e0,stroke:#fb8c00
----

{empty} +

=== Step 4: Create a Listener to Expose the Database Locally

The Listener creates a local service in your namespace that forwards traffic to the remote PostgreSQL database through the encrypted link.

{blank}

[.copypaste]
****
cat <<EOF | oc apply -f -
apiVersion: skupper.io/v2alpha1
kind: Listener
metadata:
  name: postgres-listener
  namespace: {username}-devspaces
spec:
  routingKey: postgres
  host: postgres
  port: 5432
EOF
****

IMPORTANT: The `routingKey` must match the one configured on the Connector in the `shared-database` namespace. The platform team set it to `postgres`.

Verify the Listener is ready:

{blank}

[.copypaste]
****
oc get listener postgres-listener -n {username}-devspaces
****

You should see output similar to:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">NAME                ROUTING KEY   PORT   HOST       STATUS   HAS MATCHING CONNECTOR   MESSAGE
postgres-listener   postgres      5432   postgres   Ready    true                     OK</pre>
++++

When a Listener is created, Service Interconnect automatically exposes the backend service as a local service in your environment:

{blank}

[.mermaid]
----
flowchart RL

    subgraph S2 [" "]
        Listener((Listener))
    end
    Listener -.->|creates| SVC2((svc))

    style S2 fill:#c8e6c9,stroke:#66bb6a
    style SVC2 fill:#e8f5e9,stroke:#66bb6a
    style Listener fill:#e8f5e9,stroke:#66bb6a
----

Verify the `postgres` service is available:

{blank}

[.copypaste]
****
oc get svc postgres -n {username}-devspaces
****

You should see the service available locally:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: white">NAME       TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)    AGE
postgres   ClusterIP   172.31.121.30   &lt;none&gt;        5432/TCP   21s</pre>
++++

All resources are in place:

{blank}

[.mermaid]
----
flowchart LR
    S1["✅ Site(1)"] --> C["✅ Connector"] --> S2["✅ Site(2)"] --> G["✅ AccessGrant"] --> T["✅ AccessToken"] --> L["✅ Listener"]
    style S1 fill:#e8f5e9,stroke:#66bb6a
    style C fill:#e8f5e9,stroke:#66bb6a
    style S2 fill:#e8f5e9,stroke:#66bb6a
    style G fill:#e8f5e9,stroke:#66bb6a
    style T fill:#e8f5e9,stroke:#66bb6a
    style L fill:#e8f5e9,stroke:#66bb6a
----

{empty} +

=== Step 5: Connect to the Database

You can now connect to PostgreSQL as if it were running locally. Let's use a Camel route to query the database:

{blank}

[cols="1a,1a"]
|===
| *Diagram* | *Camel Route (YAML)*

|
[.mermaid]
----
flowchart LR
    T(timer) --> SQL(sql:SELECT) --> L(log)
----

|
[source,yaml]
....
- route:
    id: query-invoices
    from:
      uri: timer:queryInvoices
      parameters:
        repeatCount: 1
      steps:
        - to:
            uri: sql:SELECT count(*) FROM invoices
        - log:
            message: "${body}"
....

|===

Run the route:

{blank}

[.copypaste]
****
[.path]
/credentials/im

camel run query/* \
--property=spring.datasource.username={username} \
--property=spring.datasource.password=REPLACE
****

The last line — `[{count=1}]` — confirms the Camel route successfully connected to the database and ran the query. The output should be similar to:

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1; white-space: pre-wrap"><span style="color:grey;font-family: monospace;font-size: 12px;">2026-03-20 13:09:41.662 <span style="color: limegreen">INFO</span> <span style="color: deeppink">4330</span> --- [           main] <span style="color: cyan">.impl.engine.AbstractCamelContext</span><span style="color: white">  : Apache Camel 4.18.0 (db) started in 263ms (build:0ms ...)</span>
2026-03-20 13:09:42.579 <span style="color: limegreen">INFO</span> <span style="color: deeppink">4330</span> --- [//queryInvoices] <span style="color: cyan">com.zaxxer.hikari.HikariDataSource</span><span style="color: white"> : HikariPool-1 - Starting...</span>
2026-03-20 13:09:42.760 <span style="color: limegreen">INFO</span> <span style="color: deeppink">4330</span> --- [//queryInvoices] <span style="color: cyan">com.zaxxer.hikari.pool.HikariPool</span><span style="color: white">  : HikariPool-1 - Added connection [class name]</span>
2026-03-20 13:09:42.762 <span style="color: limegreen">INFO</span> <span style="color: deeppink">4330</span> --- [//queryInvoices] <span style="color: cyan">com.zaxxer.hikari.HikariDataSource</span><span style="color: white"> : HikariPool-1 - Start completed.</span>
2026-03-20 13:09:42.814 <span style="color: limegreen">INFO</span> <span style="color: deeppink">4330</span> --- [//queryInvoices] <span style="color: cyan">db.yaml:10</span><span style="color: white">                         : <span style="background-color: yellow; color: black">[{count=1}]</span></span>
</span></pre>
++++

{empty} +

=== Alternative Step 5: Configure and query

Instead of passing credentials as command-line properties, you can store them in `application.properties` so they're available to all Camel routes automatically.

Open `application.properties` in your editor and add the following lines:

{blank}

[.copypaste]
****
[.path]
/credentials/im

# Datasource credentials
spring.datasource.username={username}
spring.datasource.password=REPLACE
****

Save the file. Now you can query the database using Camel's `cmd` tool without passing credentials on the command line:

{blank}

[.copypaste]
****
camel cmd send \
--uri "sql:SELECT count(*) FROM invoices" \
--properties application.properties \
--reply
****

[TIP]
--
.What does this command do?
[%collapsible]
====
* `camel cmd send` — sends a message to a Camel endpoint from the command line
* `--uri "sql:SELECT count(*) FROM invoices"` — runs a SQL query against the configured database
* `--properties application.properties` — loads database connection settings (including the credentials you just added)
* `--reply` — waits for and displays the response
====
--

You should see output similar to:

{blank}

++++
<pre style="background-color: #272822; padding: 10px 15px; line-height: 1.4; white-space: pre-wrap; font-family: monospace; font-size: 14px; color: #999"><span style="color:#999">2026-03-23 10:14:08.939</span>  <span style="color:#5fafff">11530</span> <span style="color:#999">---</span> <span style="color:#5fdfdf">sql://SELECT%20count(*)%20FROM%20invoice</span> : <span style="color:#fff">Reply received</span> <span style="color:#a6e22e">(success)</span> <span style="color:#999">(304ms)</span>
 <span style="color:#fff">Endpoint</span>  sql://SELECT%20count(*)%20FROM%20invoices
 <span style="color:#fff">Exchange</span>  <span style="color:#999">(DefaultExchange)</span>  <span style="color:#a6e22e">InOut</span>   <span style="color:#fff">E3D444D7E02ADCD-0000000000000000</span>
 <span style="color:#fff">Message</span>   <span style="color:#999">(DefaultMessage)</span>
 <span style="color:#fff">Header</span>    <span style="color:#999">(Integer)</span>             <span style="background-color: yellow; color: black">CamelSqlRowCount  1</span>
 <span style="color:#fff">Body</span>      <span style="color:#999">(ArrayList) (size: 1 bytes: 11)</span>
 <span style="background-color: yellow; color: black">[{count=1}]</span></pre>
++++

{empty} +

=== Summary

Here's the full picture of what you've built:

{blank}

[.mermaid]
[subs="attributes"]
----
flowchart LR
    subgraph yoursLabel3 ["{username}-devspaces"]
        subgraph yours3 [" "]
            Client[Camel Route]
            SVC2((svc))
            subgraph S2 ["✅ Site(2)"]
                Listener((Listener))
                Grant[AccessGrant]
            end
        end
    end

    Client --> SVC2 --> Listener

    S2 -.-|"🔒 secure link"| S1

    subgraph sharedLabel3 [shared-database]
        subgraph shared3 [" "]
            subgraph S1 ["✅ Site(1)"]
                Connector((Connector))
                Token[AccessToken]
            end
            SVC((svc))
            SVC --- DB[(PostgreSQL)]
        end
    end

    Connector --> SVC
    Grant ~~~ Token

    style yoursLabel3 fill:none,stroke:none
    style sharedLabel3 fill:none,stroke:none
    style S2 fill:#c8e6c9,stroke:#66bb6a
    style S1 fill:#c8e6c9,stroke:#66bb6a
    style Grant fill:#e8f5e9,stroke:#66bb6a
    style Listener fill:#e8f5e9,stroke:#66bb6a
    style SVC2 fill:#e8f5e9,stroke:#66bb6a
    style Token fill:#e8f5e9,stroke:#66bb6a
    style Connector fill:#e8f5e9,stroke:#66bb6a
    style DB fill:#336,color:#fff
----

You have successfully:

* Created a Site, AccessGrant, and Listener in your environment
* Created an AccessToken in the database environment to establish the link
* Used a Camel route to query the database through the encrypted link

The database lives in a secure, air-tight environment — the connection was initiated *from* the database side *to* yours through an encrypted Skupper link. No network policy changes, no VPNs, no elevated permissions required.

The link you've established will stay active — you'll need it in the next exercise to implement the full data flow: ingesting Purchase Orders, storing them in the database, and rendering PDF invoices.

{empty} +

[.verification]
****
Did the Camel route output show `[{count=1}]` confirming a successful database query?

[.success]
Well done — your database connectivity is ready. Continue to the next exercise.

[.fail]
Review your Service Interconnect resources and ensure the link status is `Ready` before running the Camel route again.
****
